{
  "name": "@faster-crud/trpc",
  "version": "0.1.0",
  "description": "tRPC adapter for @faster-crud",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "dev": "tsc -w"
  },
  "peerDependencies": {
    "@faster-crud/core": "^0.2.0",
    "@trpc/server": "^11.0.0",
    "zod": "^3.25.0 || ^4.0.0"
  },
  "devDependencies": {
    "@faster-crud/core": "workspace:*",
    "@trpc/server": "^11.0.0",
    "typescript": "^5.4.0",
    "zod": "^3.25.0 || ^4.0.0"
  }
}

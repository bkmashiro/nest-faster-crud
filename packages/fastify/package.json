{
  "name": "@faster-crud/fastify",
  "version": "0.2.1",
  "description": "Fastify adapter for @faster-crud",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc -w",
    "test": "jest --passWithNoTests"
  },
  "dependencies": {
    "fastify-plugin": "^4.5.1"
  },
  "peerDependencies": {
    "fastify": "^4.0.0",
    "@faster-crud/core": "^0.2.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "fastify": "^4.0.0",
    "@faster-crud/core": "workspace:*"
  }
}
